# URL publique de l'API (utilisée par le navigateur)
NEXT_PUBLIC_API_URL="http://localhost:4000"
# URL interne utilisée par le rendu serveur (souvent identique)
API_URL="http://localhost:4000"
# URL du site, pour les métadonnées SEO
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Code de vérification Google Search Console (méthode « balise HTML ».)
# Coller uniquement la valeur du content=, pas la balise entière. Vide = pas de balise.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=""
